services:
  asn-manager:
    build: .
    container_name: asn-manager
    ports:
      - "3000:3000"
    volumes:
      - asn-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - DB_PATH=/app/data/asn.db

volumes:
  asn-data:
    driver: local
